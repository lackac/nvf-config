diff --git a/lua/codediff/ui/explorer/actions.lua b/lua/codediff/ui/explorer/actions.lua
index e4fbadc..0d0626b 100644
--- a/lua/codediff/ui/explorer/actions.lua
+++ b/lua/codediff/ui/explorer/actions.lua
@@ -287 +287 @@ end
-function M.toggle_stage_file(git_root, file_path, group)
+function M.toggle_stage_file(git_root, file_path, group, done)
@@ -301,0 +302,10 @@ function M.toggle_stage_file(git_root, file_path, group)
+  local function complete(err)
+    if err then
+      vim.schedule(function()
+        vim.notify(err, vim.log.levels.ERROR)
+      end)
+    elseif done then
+      done()
+    end
+  end
+
@@ -304,7 +314 @@ function M.toggle_stage_file(git_root, file_path, group)
-    git.unstage_file(git_root, file_path, function(err)
-      if err then
-        vim.schedule(function()
-          vim.notify(err, vim.log.levels.ERROR)
-        end)
-      end
-    end)
+    git.unstage_file(git_root, file_path, complete)
@@ -313,7 +317 @@ function M.toggle_stage_file(git_root, file_path, group)
-    git.stage_file(git_root, file_path, function(err)
-      if err then
-        vim.schedule(function()
-          vim.notify(err, vim.log.levels.ERROR)
-        end)
-      end
-    end)
+    git.stage_file(git_root, file_path, complete)
@@ -322,7 +320 @@ function M.toggle_stage_file(git_root, file_path, group)
-    git.stage_file(git_root, file_path, function(err)
-      if err then
-        vim.schedule(function()
-          vim.notify(err, vim.log.levels.ERROR)
-        end)
-      end
-    end)
+    git.stage_file(git_root, file_path, complete)
diff --git a/lua/codediff/core/git/runner.lua b/lua/codediff/core/git/runner.lua
index 57d0aac..d8b8b1f 100644
--- a/lua/codediff/core/git/runner.lua
+++ b/lua/codediff/core/git/runner.lua
@@ -41 +41 @@ function M.run_async(args, opts, callback)
-    }, function(result)
+    }, vim.schedule_wrap(function(result)
@@ -47 +47 @@ function M.run_async(args, opts, callback)
-    end)
+    end))
diff --git a/lua/codediff/ui/explorer/refresh/init.lua b/lua/codediff/ui/explorer/refresh/init.lua
index 2924f5c..41ec212 100644
--- a/lua/codediff/ui/explorer/refresh/init.lua
+++ b/lua/codediff/ui/explorer/refresh/init.lua
@@ -201,2 +201,3 @@ function M._refresh_once(explorer, done, force)
-  -- Skip refresh if explorer is hidden
-  if explorer.is_hidden then
+  -- Background refreshes can wait until the explorer is visible, but an
+  -- explicit refresh after staging must also update hidden sessions.
+  if explorer.is_hidden and not force then
@@ -207,2 +208 @@ function M._refresh_once(explorer, done, force)
-  -- Verify window is still valid before accessing
-  if not vim.api.nvim_win_is_valid(explorer.winid) then
+  if not explorer.is_hidden and not vim.api.nvim_win_is_valid(explorer.winid) then
@@ -219 +219,3 @@ function M._refresh_once(explorer, done, force)
-      if lifecycle.get_panel_view(explorer.tabpage) ~= explorer or not vim.api.nvim_win_is_valid(explorer.winid) then
+      if lifecycle.get_panel_view(explorer.tabpage) ~= explorer
+        or (not explorer.is_hidden and not vim.api.nvim_win_is_valid(explorer.winid))
+      then
@@ -263,0 +266,2 @@ function M._refresh_once(explorer, done, force)
+          local session = lifecycle.get_session(explorer.tabpage)
+          local advance_hunk = session and session.pending_cursor_landing ~= nil
@@ -272 +276 @@ function M._refresh_once(explorer, done, force)
-          }, { no_jump = true, force = force })
+          }, { no_jump = not advance_hunk, force = force })
diff --git a/lua/codediff/ui/view/actions/hunk.lua b/lua/codediff/ui/view/actions/hunk.lua
index 69de9ff..c85642f 100644
--- a/lua/codediff/ui/view/actions/hunk.lua
+++ b/lua/codediff/ui/view/actions/hunk.lua
@@ -113,0 +114,6 @@ function M.stage_hunk(ctx)
+    local explorer = lifecycle.get_panel_view(ctx.tabpage)
+    local current_session = lifecycle.get_session(ctx.tabpage)
+    if explorer and current_session then
+      current_session.pending_cursor_landing = hunk_idx
+      require("codediff.ui.explorer.refresh").refresh(explorer, nil, true)
+    end
diff --git a/lua/codediff/ui/view/actions/stage.lua b/lua/codediff/ui/view/actions/stage.lua
index aaf26d8..2641a4b 100644
--- a/lua/codediff/ui/view/actions/stage.lua
+++ b/lua/codediff/ui/view/actions/stage.lua
@@ -53 +53,3 @@ function M.toggle_stage(ctx)
-    explorer_module.toggle_stage_file(explorer.git_root, file_path, group)
+    explorer_module.toggle_stage_file(explorer.git_root, file_path, group, function()
+      require("codediff.ui.explorer.refresh").refresh(explorer, nil, true)
+    end)
diff --git a/lua/codediff/ui/view/inline_view/render.lua b/lua/codediff/ui/view/inline_view/render.lua
index e3a9962..07d341d 100644
--- a/lua/codediff/ui/view/inline_view/render.lua
+++ b/lua/codediff/ui/view/inline_view/render.lua
@@ -40 +40,3 @@ function M.compute_and_render_inline(modified_buf, original_buf, original_lines,
-      local target_line = landing == "last" and lines_diff.changes[#lines_diff.changes].modified.start_line or lines_diff.changes[1].modified.start_line
+      local hunk_index = type(landing) == "number" and math.min(landing, #lines_diff.changes)
+        or (landing == "last" and #lines_diff.changes or 1)
+      local target_line = lines_diff.changes[hunk_index].modified.start_line
diff --git a/lua/codediff/ui/view/render.lua b/lua/codediff/ui/view/render.lua
index 45a1813..5dff181 100644
--- a/lua/codediff/ui/view/render.lua
+++ b/lua/codediff/ui/view/render.lua
@@ -124 +124,3 @@ function M.compute_and_render(
-        local hunk = landing == "last" and lines_diff.changes[#lines_diff.changes] or lines_diff.changes[1]
+        local hunk_index = type(landing) == "number" and math.min(landing, #lines_diff.changes)
+          or (landing == "last" and #lines_diff.changes or 1)
+        local hunk = lines_diff.changes[hunk_index]
